version: '3.9'

services:
  web:
    build: .
    env_file:
      - .env
    command: >
      sh -c "python manage.py collectstatic --noinput &&
             gunicorn AgentTrader.wsgi:application --bind 0.0.0.0:8000"
    ports:
      - "8000:8000"
